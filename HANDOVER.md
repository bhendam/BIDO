# دفتر السماكة — وثيقة تسليم المشروع

> **الغرض من الملف:** أي مساعد AI أو مطوّر يقرأه يقدر يكمّل الشغل من غير ما يرجع
> لأي محادثة سابقة. الوثيقة تصف الحالة الفعلية للكود، لا النوايا.
>
> **آخر نسخة موثّقة:** `APPVER = 4.20.0` · `sw.js VERSION = v4.20.0`
> **حجم الملف:** ~5090 سطر في `index.html` واحد · 361 ك.ب
>
> **الوثيقة كانت متأخرة عن الكود** (كانت مكتوبة 4.1.0 والواقع 4.6.3، و«٤٤ اختبار»
> والواقع ٦٢). اتصلّحت في 4.8.0. **حدّثها مع كل دفعة، مش في آخر الشغل.**

---

## ١. هدف التطبيق

نظام إدارة **تجارة أسماك بالعمولة** في مصر. المكتب وسيط بين:

- **المزارع** — مصدر السمك، تستحق ثمن بضاعتها ناقص عمولة المكتب.
- **المبايع** (الصالات/التجار) — تشتري وتدفع لاحقاً، فعليها مديونية.
- **السائقين** — ينقلون بأجر، وقد يأخذون سُلفاً.
- **مصانع الثلج** — تورّد بلاطات ثلج للتبريد.

المستخدم الأساسي: **أمين المخزن** — يسجّل من موبايله في السوق فجراً (بيئة رطبة،
إضاءة سيئة، إيد واحدة، سرعة عالية: قد تتجاوز ٥٠ عملية في اليوم).
المستخدم الثاني: **صاحب العمل** — يقرأ التقارير ويعتمد التصفيات.

**المشكلة المحورية اللي التطبيق بيحلّها:** الأرقام في هذه التجارة مصدر خلاف دائم.
فالتطبيق مبني على أن **كل رقم يجب أن يكون قابلاً للتتبّع إلى حركة مؤرّخة**،
ولا يوجد رقم يُكتب يدوياً في خانة رصيد.

---

## ٢. المعمارية

### ٢.١ الشكل العام

- **ملف HTML واحد** (`index.html`) فيه CSS و JavaScript مضمّنين. لا فريمووركس ولا خطوة بناء.
- **PWA** يعمل بدون إنترنت: `manifest.json` + `sw.js` + ٣ أيقونات PNG.
- **التخزين:** IndexedDB داخل الجهاز. لا سيرفر ولا حساب ولا تتبّع.
- **الواجهة:** عربية RTL بالكامل، مصمّمة للموبايل أولاً.

### ٢.٢ السجل التراكمي (Append-only Ledger) — الأساس

**مخزن `ledger` يُكتب فيه بـ `add()` فقط. لا `put` ولا `delete` إطلاقاً.**

كل تصحيح يتم بإضافة حركة جديدة:

| الفعل | التنفيذ |
|---|---|
| حذف حركة | إضافة `{type:'void', voidOf:<id>}` |
| تعديل حركة | إضافة نسخة جديدة تحمل `replaces:<id>` |
| إلغاء تصفية | إضافة `{type:'settlement_reversal', refId:<id>}` |
| تصحيح جرد | إضافة حركة بكش بأرجل الفرق |

**الحركات الميتة** تُحسب في `derive()`:

```js
const dead=new Set();
ledger.forEach(e=>{ if(e.voidOf)dead.add(e.voidOf); if(e.replaces)dead.add(e.replaces) });
const live = ledger.filter(e=>e.type!=='void' && !dead.has(e.id));
```

كل الحسابات تقرأ `live` فقط. الأصل يبقى في القرص للمراجعة.

### ٢.٣ سلسلة التجزئة (Hash Chain)

كل حركة تحمل `prevHash` و `hash`:

```js
rec.prevHash = S.chainTip || "";
rec.hash = await sha(rec.prevHash + bodyOf(rec));   // SHA-256 عبر SubtleCrypto
```

`bodyOf()` ينتج تمثيلاً ثابتاً (مفاتيح مرتّبة، بلا `seq`/`hash`/`prevHash`)
حتى تتطابق التجزئة عند إعادة الفحص. أي تعديل مباشر على IndexedDB يكسر السلسلة
ويظهر في **الإعدادات ← الأمان ← فحص سلسلة التوقيع**.

عند غياب `crypto.subtle` (سياق غير آمن) يسقط الكود على تجزئة FNV بسيطة مع بادئة `fnv`.

### ٢.٤ تدفّق البيانات

```
IndexedDB ──getAll──> S.ledger + S.entities
                          │
                     derive(entities, ledger)   ← دالة نقية، لا آثار جانبية
                          │
                        S.D  (كل الأرصدة والقوائم المشتقّة)
                          │
                      render()  ← يبني HTML كنص ويحقنه في #app
```

- `append()` يكتب ثم **يضيف السجل للذاكرة مباشرة** (`S.ledger.push`) ويعيد الاشتقاق،
  بدل إعادة قراءة السجل كاملاً من القرص عند كل كتابة.
- `derive()` **دالة نقية** — نفس المدخلات تعطي نفس المخرجات. عليها ٦٢ اختباراً.

### ٢.٥ الملفات

```
index.html        التطبيق كامل (CSS + JS مضمّنان)
sw.js             عامل الخدمة — يخزّن ملفات التطبيق فقط، لا البيانات
manifest.json     بيان PWA (فيه أيقونات ملفات + أيقونات data-URI احتياطية)
icon-192.png · icon-512.png · maskable-512.png
README.md         دليل المستخدم والنشر
```

**كل الملفات في مستوى واحد بلا مجلدات** — قرار مقصود لأن المستخدم يرفع من الموبايل
وGitHub لا يحافظ على المجلدات في الرفع بالسحب من الهاتف.

---

## ٣. هيكل البيانات (Schema)

### ٣.١ مخازن IndexedDB

| المخزن | المفتاح | ملاحظات |
|---|---|---|
| `ledger` | `seq` (autoIncrement) | فهارس على `type` و `date`. **إضافة فقط**. |
| `entities` | `id` | يقبل `put` — بيانات تعريفية لا حسابات |
| `meta` | `k` | إعدادات: `company`, `security`, `backup`, `deviceId`, `autoBox`, `wipeLog` |

### ٣.٢ الحقول المشتركة لكل حركة

```js
{
  seq,          // ترتيب زمني ثابت من IndexedDB
  id,           // معرّف فريد (uid)
  opId,         // معرّف عملية — للمزامنة ومنع التكرار
  dev,          // معرّف الجهاز
  by,           // 'owner' أو 'staff'
  createdAt,    // ISO timestamp
  date,         // 'YYYY-MM-DD' — التاريخ المحاسبي، غير createdAt
  prevHash, hash,
  note,
  replaces?,    // معرّف الحركة التي تستبدلها
}
```

### ٣.٣ أنواع الحركات والحقول المطلوبة

```js
const SCHEMA = {
  invoice:              ['date','farmId','merchantId'],
  collection:           ['date','merchantId'],
  trip:                 ['date','driverId'],
  driver_advance:       ['date','driverId'],
  driver_payment:       ['date','driverId'],
  factory_payment:      ['date','factoryId'],
  ice_op:               ['date','factoryId','farmId'],
  settlement:           ['date','farmId'],
  settlement_reversal:  ['refId'],
  box:                  ['date','op'],
  void:                 ['voidOf'],
  opening_cash:         ['date','entityId','amount'],
  driver_damage_clear:  ['date','driverId','count']
};
// حقول يجب أن تكون > 0 — لاحظ أن invoice غير مذكورة عمداً (المسوّدة مبلغها صفر)
const POSF = { collection:['amount'], driver_advance:['amount'],
  driver_payment:['amount'], factory_payment:['amount'], ice_op:['qty'] };
```

`validate()` تعمل داخل `append()` نفسها، فلا تدخل حركة مشوّهة من أي باب
(نموذج · استيراد نسخة · مزامنة).

### ٣.٤ الحقول الخاصة بكل نوع

| النوع | الحقول |
|---|---|
| `invoice` | `farmId, merchantId, driverId?, boxes, amount, note` — **amount=0 يعني مسوّدة** |
| `collection` | `merchantId, amount` |
| `settlement` | `farmId, total, office, farmRatio, pct, commission, expenses, net, prevDebt, payable, boxes, coveredIds[]` |
| `trip` | `driverId, routeId, fare` |
| `driver_advance` / `driver_payment` | `driverId, amount` |
| `factory_payment` | `factoryId, amount` |
| `ice_op` | `factoryId, farmId, qty, unitPrice` |
| `opening_cash` | `entityId, entityType, amount` (يقبل السالب) |
| `driver_damage_clear` | `driverId, count` |
| `box` | `op, legs[], params{}, driverId?, dmgReason?, invoiceId?, auto?, cost?` |

### ٣.٥ الجهات (entities)

```js
{ id, type, name, place?, phone?, price?, fare?, deleted?, createdAt }
```

| النوع | الحقول | رصيد افتتاحي |
|---|---|---|
| `farm` | name, place, phone | **بكش فقط** |
| `merchant` | name, place, phone | نقدية + بكش |
| `driver` | name, phone | نقدية فقط |
| `route` | name, fare (أجرة افتراضية) | — |
| `ice` | name, place, price (سعر البلاطة), phone | نقدية فقط |

الحذف **ناعم** (`deleted:true`) — يختفي من القوائم ويبقى اسمه في التقارير القديمة.

### ٣.٦ نموذج البكش — دفتر مواقع مزدوج الحالة

كل حركة بكش = مجموعة **أرجل**:

```js
leg = { from, to, cond, count }
cond ∈ { 'intact' (سليم), 'damaged' (تالف) }
```

**المواقع الحقيقية:** `'WH'` (المخزن) · معرّف أي جهة.
**المواقع الافتراضية** (تبدأ بـ `#`، تُقفل المعادلة ولا تُعرض كأرصدة):

```js
'#PURCHASE' شراء جديد · '#RECYCLE' إعادة تدوير · '#LOSS' فاقد
'#EXT' خارج السجل     · '#ADJ' تصحيح رصد     · '#OPEN' رصيد افتتاحي
'#SUPPLY' مصدر الاستبدال
```

الرصيد = مجموع الداخل − مجموع الخارج لكل (موقع، حالة).

**أرجل كل عملية:**

| العملية | الأرجل |
|---|---|
| `issue_farm` | `WH → farm` سليم |
| `scrap_out` | `WH → #RECYCLE` تالف |
| `return_in` | `from → WH` سليم(okCount) **+** `from → #ADJ` سليم(dmgCount) **+** `#ADJ → WH` تالف(dmgCount) |
| `factory_replace` | `WH → #SUPPLY` تالف **+** `#SUPPLY → WH` سليم |
| `purchase_in` | `#PURCHASE → WH` سليم |
| `sale_move` | `farm → merchant` سليم |
| `empty_back` | `merchant → farm` سليم |
| `farm_swap` | `farm → farm` سليم |
| `fix_d2i` / `fix_i2d` | داخل `WH` فقط عبر `#ADJ` |
| `out_noone` | `loc → #LOSS` (سليم إجباراً خارج المخزن) |
| `in_nosource` | `#EXT → loc` |
| `opening_box` | `#OPEN → loc` (أو العكس للرصيد السالب) |
| `audit` | أرجل الفرق آلياً: العجز `loc → #LOSS` · الزيادة `#EXT → loc` |

---

## ٤. القواعد المحاسبية

### ٤.١ القاعدة الحاكمة

> **الأرصدة لا تُخزَّن ولا تُعدَّل يدوياً أبداً. تُحسب لحظياً من مجموع الحركات.**

```js
رصيد المبيع  = افتتاحي + مجموع الفواتير − مجموع التحصيلات
رصيد المزرعة = افتتاحي + مجموع الفواتير − مجموع التصفيات النشطة
رصيد السائق  = افتتاحي + أجور الرحلات − السلف − المدفوعات
رصيد المصنع  = افتتاحي + (بلاطات × سعر) − المدفوعات
```

**اتجاه الإشارة موحّد:** الموجب دائماً «مستحق» في الاتجاه الطبيعي للجهة
(على المبيع · للمزرعة · للسائق · للمصنع)، والسالب هو العكس.

### ٤.٢ حساب المال بالقروش الصحيحة

كل جمع وطرح مالي يتم بأعداد صحيحة:

```js
const P = v => Math.round((+v||0)*100);        // إلى قروش
const sumM = (a,f) => a.reduce((t,x)=>t+P(f(x)),0)/100;
const msum = (...v) => v.reduce((t,x)=>t+P(x),0)/100;   // msum(a,-b,-c)
```

**السبب:** `0.1+0.2 = 0.30000000000000004` بالعشري. مع آلاف السطور يظهر الفرق
في المجاميع، وهو بالضبط نوع الخطأ الذي يولّد خلافاً مع المزارع.

### ٤.٣ دورة المزرعة والتصفية

- **الدورة** = الفواتير غير المغطّاة بأي تصفية نشطة.
- عند الاعتماد تُسجَّل حركة `settlement` تحمل `coveredIds[]` = بصمة الفواتير المُقفلة.
- الرصيد يصبح صفراً حسابياً (فواتير − تصفيات)، **بلا حذف أي سطر**.
- **التراجع** يضيف `settlement_reversal` فتعود نفس الفواتير للدورة الجارية.
- **دورة جديدة لمزرعة مصفّاة** مستقلة تماماً عن السابقة (دورة سمك جديدة).

### ٤.٤ العمولة — كسر بسط/مقام

```js
const ratioPct = (a,b) => b>0 ? a/b*100 : 0;
commission = round50( total × (البسط ÷ المقام) )   // round50 = تقريب لأقرب ٥٠
net        = total − commission − expenses
payable    = net − prevDebt
```

- تُكتب على طريقة السوق: **٤/٩٦** (= ٤٫١٦٧٪) أو **٥/٧٠** (= ٧٫١٤٣٪).
- **لا يُشترط أن يكون المجموع ١٠٠.**
- التقريب لأقرب ٥٠ جنيه عُرف السوق (تفادي الكسور في العدّ النقدي).
- الحقل **قابل للتعديل اليدوي**: أول لمسة ترفع `commissionTouched` فيتوقف الحساب الآلي.
- `payable` قد يخرج سالباً (المزرعة مدينة للمكتب) — **يُعرض كما هو ولا يُصفَّر**.

### ٤.٥ الفصل بين الحسابات والعمليات

| المجال | يؤثر على |
|---|---|
| الفواتير والتحصيلات والتصفيات | مديونية المبايع · مستحقات المزارع |
| البكش والثلج والسائقين | **لا يمسّ** مديونية مبيع ولا مستحق مزرعة |

حسابات السائقين والمصانع حسابات **موردين** — منفصلة عن حسابات المبيعات والمزارع.

### ٤.٦ قاعدة عهدة البكش

> **عهدة الجهات «سليم» فقط. التالف لا يوجد إلا في المخزن.**

الصندوق خرج للجهة سليماً، فيُخصم من عهدتها **سليماً** حتى لو رجع تالفاً،
ويدخل المخزن **تالفاً**. لذلك `return_in` يمرّ بعقدة `#ADJ`.

- **الرصيد السالب مسموح** للمبايع والمزارع: بكش يخصّها محتفظين به عندنا.
- خانة التالف في الجرد تظهر **للمخزن وحده**.
- تصحيح الحالة (تالف ↔ سليم) **داخل المخزن وحده**.

### ٤.٧ عدّاد تالف السائقين

عند `return_in` يُختار السائق وسبب التالف:

| السبب | الأثر |
|---|---|
| `wear` (هلك عادي) | الصناديق تدخل المخزن · **لا شيء على السائق** |
| `negligence` (إهمال) | الصناديق تدخل المخزن **أيضاً** · ويُسجَّل الرقم على السائق **كعدّاد متابعة** |

> **السائق ليس له عهدة بكش إطلاقاً.** العدّاد **رقم متابعة فقط، ليس مديونية**،
> ولا يدخل حسابه المالي. يُصفَّر بحركة `driver_damage_clear` مؤرّخة،
> **والتصفية لا تُرجع الصناديق** من المخزن.

### ٤.٨ الفاتورة المسوّدة

فاتورة بلا مبلغ (`amount = 0`):

- **تُستبعد من كل حساب مالي** — لا تدخل مديونية ولا مستحقات.
- **البكش ينتقل فعلاً** من المزرعة للمبيع (لأنه تحرّك في الواقع).
- تظهر في «مسوّدات» وفي «محتاج انتباهك» على الرئيسية.
- تتحوّل لفاتورة كاملة بكتابة المبلغ (حركة `replaces`).

### ٤.٩·أ التراجع السريع عن آخر حركة

`S.flash = {id, until}` — يُضبط داخل `append()` لكل حركة **ليست** `void`.

- يظهر زر «تراجع» على صف الحركة في «آخر ٣» **٢٠ ثانية فقط**.
- الشرط أضيق من الحذف العام: آخر حركة سجّلها **هذا الجهاز في هذه الجلسة**،
  فلا يحتاج الرقم السري. الحالة في الذاكرة فقط ولا تعيش بعد إغلاق التطبيق.
- **ليس حذفاً:** `undoLast()` يضيف `{type:'void', voidOf:id}` تماماً كزر الحذف،
  ويسحب معه ترحيل البكش الآلي المرتبط، ويمرّ على `guardEdit` وقفل الفترات.
- مؤقّت يمسح الوسم بعد ٢٠ ثانية، **ولا يعيد الرسم إن كان المستخدم داخل نموذج**
  (الرسم يهدم الـDOM).

### ٤.٩ الترحيل الآلي للبكش

عند حفظ فاتورة، تُنشأ حركة `sale_move` مستقلة مربوطة بها عبر `invoiceId` وعلامة `auto`.
تتبع الفاتورة في التعديل والحذف. يمكن إيقافه من الإعدادات.

---

## ٥. شكل الواجهة

### ٥.١ التنقّل — ٤ تبويبات

| التبويب | المحتوى |
|---|---|
| **الرئيسية** | «محتاج انتباهك» ثم ٤ أقسام: الحسابات · البكش · السائقين · الثلج |
| **العمليات** | حسابات · بكش · سائقين · ثلج (كل التسجيل اليومي) |
| **التقارير** | ٩ تقارير خلف زر اختيار واحد |
| **الإعدادات** | البيانات · الشركة · الأمان · النسخ |

**زر «+» عائم** في الرئيسية والعمليات → ٤ خيارات طافية تنقل لصفحة عمليات القسم.
**زر 🔎 في الترويسة** → بحث موحّد في الجهات والحركات.

### ٥.٢ العمليات ← بكش

مجموعات: **صادر · وارد · تحويلات · جرد وتسويات · الأرصدة**، وداخل كل مجموعة
تبويب لكل عملية على حدة. العمليات النادرة (تصحيح الرصد · بلا طرف) مخفية خلف زر.

### ٥.٣ التقارير التسعة

| المجموعة | التقارير |
|---|---|
| **يومي** | تسليم اليوم (الافتراضي) · المخزن اليومي |
| **جهة معيّنة** | كشف حساب · عهدة البكش · السائقين · الثلج |
| **مراجعة** | الجرد · تالف السائقين · كل العمليات |

الشاشة: **رأس التقرير + سطر فلتر مطوي + النتيجة + أزرار الطباعة**.

### ٥.٤ نظام الألوان

> **اللون نادر ومعناه ثابت.**

- **القماشة رمادية محايدة** — الأرقام الملوّنة هي التي تبرز.
- **لون الفعل ثابت** لا يتغيّر بين الأقسام.
- **الاتجاه لونان فقط:** 🟢 داخل · دائن · له — 🔴 خارج · مدين · عليه.
- **هوية القسم** في الأيقونة والشريط العلوي فقط، **لا في الخلفية**.
- ألوان المجالات: حسابات `#0E6E7D` · **بكش `#5E45AC` بنفسجي** · سائقين `#8F6200` · ثلج `#15628F`.
  البنفسجي مقصود ليفصل «مجال البكش» عن «التالف = أحمر».
- كل حركة عليها **نقطة لونية** تعرّف مجالها.
- **كل الألوان مفحوصة:** أقل تباين **4.76:1**، أعلى **17.6:1**.
- **الوضع الغامق:** تلقائي من النظام أو يدوي من الإعدادات (`data-mode` على `<html>`)،
  ولون شريط المتصفح (`theme-color`) يتبعه. السبب: الشاشة الفاتحة تبهر في السوق فجراً.

### ٥.٤.١ الأيقونات — SVG لا إيموجي

**٢٦ أيقونة** في `<symbol>` واحد أعلى `<body>` (معرّفات `#i-*`)، تُستدعى بـ`IC('name')`
التي تُخرج `<svg class="ico"><use href="#i-name"/></svg>`.

- الأيقونة تأخذ لونها من `currentColor` — لا تفرض لوناً فتكسر قاعدة «اللون نادر».
- الإيموجي يختلف شكله بين الأجهزة ويأتي ملوّناً غصباً؛ لذلك أُزيل من كل عناصر الواجهة.
- **استثناءان مقصودان:** نصّ رسالة واتساب (الإيموجي مطلوب هناك) وشاشة الطباعة
  (أحادية اللون أصلاً).
- `.ico{pointer-events:none}` — وإلا ابتلعت الضغطة قبل أن تصل إلى `data-act`.

### ٥.٤·أ تقرير السائقين

- **بلا سائق مختار:** قسم مستقل لكل سائق له حركة في المدى (عنوانه اسمه وصافي
  فترته)، وتحته حركاته برصيد جارٍ **خاص به**. ثم جدول واحد بإجمالي كل
  السائقين في المدى المختار.
- **بسائق مختار:** قسمه وحده، ثم جدول الإجمالي بسطر واحد.
- **لا يوجد قسم «أرصدة» تراكمي** — أُزيل في 4.10.0. الأرصدة الكلّية موجودة في
  **العمليات ← السائقين**.
- السائق بلا حركة في المدى لا يظهر أصلاً.
- كل الحسابات بالقروش الصحيحة (`P` · `msum`) لا جمعاً عشرياً.

### ٥.٤·ب تقرير مصاريف المزارع اليومي (`costDaily`)

«صرفت كام النهارده وعلى إيه» — سؤال آخر اليوم قبل القفل.

- **يوم واحد إجباري** مثل `farmDaily` و`colDaily`؛ `selectRep` يضبط المدى على
  اليوم تلقائياً عند اختياره.
- جدول مجمّع: مزرعة في كل سطر، بند في كل عمود (ثلج · سيارات · عمال · نثريات)،
  ثم تفصيل بند ببند لكل مزرعة. المزرعة بلا مصاريف لا تظهر.
- **يقرأ من `D.live` لا من `farm.openCosts`.** `openCosts` تحتوي البنود غير
  المغطّاة بتصفية فقط، فلو صُفّيت الدورة صار التقرير التاريخي فارغاً.
- **النقدية (`cat:'cash'`) عمود مستقل.** تُحمَّل على الدورة مع التصفية
  (`cycleExpenseTotal` يشملها) لكن **شاشة قفل اليوم تعدّ البنود الأربعة وحدها**
  — خلطها في نفس العمود يجعل الرقمين مختلفين بلا تفسير، فلها عمود وملاحظة.

### ٥.٤·ج ورقة تسليم اليوم

**الورقة تبدأ من المزارع مباشرةً.** حتى 4.14.0 كانت تبدأ بشبكة مؤشّرات الفترة
والمقارنة بالفترة السابقة وجدول الأرصدة العامة — شاشة كاملة قبل أول رقم يهمّ
المستلم، وكلها موجودة في الرئيسية وفي تقاريرها. أُزيلت من هذه الورقة في 4.15.0،
و«محتاج مراجعة» **نُقل إلى آخرها ولم يُحذف**: ينبّه على مسوّدات بلا مبلغ وهي
تغيّر أرقام الورقة نفسها.

ترتيب الورقة:

1. **تفصيل المزارع** — قسم لكل مزرعة عليها حركة في المدى، الواحدة تحت الأخرى:
   **البيع** (المبيع · السائق · بكش · المبلغ) ثم **الثلج** ثم **المصاريف** ثم
   **البكش** — جدول مستقل بثلاثة أرقام: **صادر للمزرعة · وارد منها · الباقي
   عليها**، بإطار ورأس بلون البكش (`--c-box`) ليتميّز عن جداول البيع والثلج
   والمصاريف. جدول الحركات التفصيلي أُزيل (موجود في تقرير البكش).
   > **التسمية بالاتجاه صراحةً.** «صادر» بالنسبة للمزرعة عكسها بالنسبة للمخزن،
   > فكلمة «صادر» وحدها تقلب الرقمين على القارئ. في `farmSpan`: `bIn` =
   > الداخل إلى المزرعة = **صادر للمزرعة**، و`bOut` = **وارد منها**.
   > **«الباقي» = `d.close` = العهدة التراكمية** حتى آخر المدة، لا فرق الصادر
   > والوارد في المدة. ملاحظة تحت الجدول تقول ذلك صراحةً لأن القارئ يطرح
   > الرقمين تلقائياً فيجد ناتجاً مختلفاً.
   **القسم الفارغ يُحذف** ولا يُكتب «لا يوجد» — أربع رسائل فارغة تحت كل مزرعة
   تُطيل الورقة وتصعّب قراءتها. البكش وحده يظهر دائماً لأن الرصيد رقم قائم
   حتى بلا حركة. المزرعة بلا أي حركة لا تظهر أصلاً.
2. **التحصيلات** — جدول واحد بكل تحصيلات المدى وإجماليها.
3. **صافي حساب المبايع** — رصيد كل مبيع **في نهاية اليوم المختار** لا اليوم
   الحالي: `merchAsOf(to)` تحسب من كل الحركات المؤرَّخة حتى ذلك التاريخ فقط،
   بنفس معادلة `derive` (افتتاحي + مبيعات − تحصيل). فورقة تسليم يوم قديم تعرض
   الأرقام كما كانت وقتها. المسوّدات (بلا مبلغ) **مستبعدة** من الحساب وتُعدّ
   على جانب كتنبيه، ويظهر عددها كوسم بجانب اسم المبيع.
   **عمودان فقط: الاسم والصافي.** أعمدة المبيعات والتحصيل أُزيلت في 4.16.0 —
   تفصيلها في جدول التحصيلات فوقه وفي تقرير المبيع، وتكرارها هنا يضع أربعة
   أرقام في سطر الهدف منه رقم واحد. المبيع بلا أي حركة ولا رصيد لا يظهر.
   الأرصدة السالبة (دفعة مقدّمة) تُعرض ولا تُمنع (قاعدة ٨).

**نقطة الحساب واحدة:** `farmSpan(fid,from,to)` تُرجع كل أرقام المزرعة في المدى،
و`colSpan(from,to,ent)` تُرجع جدول التحصيلات. يستعملهما:
`handover()` (الشاشة) · `summaryHTML()` (الطباعة) · `repFarmDaily()` ·
`repCollDaily()`. الحساب كان **مكرّراً حرفياً** في `repFarmDaily` وأُزيل في
4.13.0. أي تعديل في المعادلة يتم في `farmSpan` وحدها.

- الطباعة تستعمل `farmsPrint()` → `farmBlockPrint` (عناوين `h3.farmsec`/`h4`
  وجداول) بدل بطاقات الشاشة — نفس الأرقام بشكل ورقي وبنفس الترتيب.
  **`summaryHTML()` منفصلة** وتخدم «طباعة ملخص» من الرئيسية فقط — لا تُدمج
  الاثنتان: الأولى ورقة تسليم تُوقَّع، والثانية ملخص فترة بمقارنات.
- يعمل مع أي مدى لا اليوم فقط: مع مدى أطول يظهر عمود التاريخ في التحصيلات،
  ورصيد البكش يُحسب من أول المدة إلى آخرها.

### ٥.٤·د الرئيسية — الأقسام المطويّة

الشاشة الأولى يجب أن تُقرأ بنظرة، فالقوائم الطويلة تُطوى خلف سطر واحد:

- **«محتاج انتباهك»** رأسه زر يطوي بنوده. مطويّ افتراضياً.
  **العدّاد يصير أحمر** (`.ab.bad`) إن كان بين البنود واحد بدرجة `bad` —
  إشارة تُقرأ والقسم مغلق، وإلا مرّ خطأ خطير أياماً خلف الطيّة.
- **قائمة العهدة (`custodyList`)**: النشط فقط ظاهر، والجهات التي عهدتها صفر
  ومعها **فاقد قديم** تُجمَّع في سطر «فاقد بلا عهدة (ن) · س صندوق» يُفتح
  بالضغط. صفٌّ بصفر كبير لجهة سلّمت كل شيء لا يحمل قراراً، وكان ثمانية منها
  تدفن الجهتين اللتين عندهما بكش فعلاً. الفاقد نفسه **لا يُحذف**: رقمه في
  إجمالي السطر وفي تقرير «فاقد البكش».
  > **الرصيد السالب يبقى نشطاً** (`intact<0` = «عندنا»): جهة أعادت أكثر مما
  > أخذت حالة تحتاج مراجعة لا حالة مغلقة.
- **المزارع المصفّاة** تُجمَّع في سطر واحد «مصفّاة (ن)» داخل تفصيل المزارع.
  النشطة تبقى ظاهرة فوقه دائماً — هي التي تحتاج قراراً. عند غياب المصفّاة
  لا يظهر السطر أصلاً.
- **كلاهما يستعمل `S.drill` الموجودة** (`data-act="drill"` + `data-key`)،
  لا آلية طيّ جديدة. الحالة في الذاكرة فتعود مطويّة مع كل فتحة.

### ٥.٤·هـ مسار التسجيل — أقل عدد لمسات

المستخدم يفتح التطبيق ليسجّل لا ليقرأ، فالمسار إلى النموذج أقصر ما يمكن:

- **صف تسجيل في أعلى الرئيسية**: فاتورة بيع · تحصيل · حركة بكش — لمسة واحدة
  إلى النموذج. مضغوط عمداً (٦٤px) **حتى تبقى بطاقة مديونية المبايع في نفس
  الشاشة**: الأرصدة مطلوبة للمستخدم ولا تُزاح.
- **أزرار الزر العائم أفعال لا أقسام**: كانت «حسابات · بكش · سائقين · ثلج»
  وهي تصل إلى نماذج مباشرة، فالاسم كان يَعِد بقائمة ويعطي نموذجاً.
- **`dateLine()`** يستبدل حقل التاريخ بسطر «النهارده · ٢٨/٠٨» وزر «تغيير».
  حقل `type=date` **موجود في الـDOM دائماً ومخفي** — لو بُني عند الضغط
  لاحتاج `render()` وهو يهدم النموذج ويضيّع ما كُتب فيه. الاسم `date` كما هو
  فلا يتغيّر `readForm`.
- **`noteField()`** يطوي الملاحظة الاختيارية خلف زر «+ ملاحظة».
- **في الفاتورة «حفظ وواحدة تانية» هو الزر الأساسي** و«حفظ وخلاص» ثانوي:
  المزرعة الواحدة تتوزّع على عدة مبايع فهذا هو المسار الطبيعي.
  > الزر الأساسي صار `type=button`، فقاعدة شريط الحفظ الملتصق كانت ستمسك
  > الزر **الثانوي** وحده. لذلك يلتصق `.btnrow.savebar` كصف كامل، والزر
  > المفرد يلتصق فقط `:not(.btnrow *)`.
- **`D.colTop`**: أكثر ٣ مبالغ تحصيل تكراراً (المدوّرة على ٥٠٠ فقط ليبقى
  الاقتراح قابلاً للتكرار) — نفس فكرة `boxTop`.
- **الصفر ليس رقماً**: صف الجهة برصيد صفر يعرض `✓` رمادية صغيرة بدل رقم
  بأكبر خط في السطر. قائمة من ١٥ صفاً كلها «0» تُمسح بالعين بلا فائدة.

### ٥.٤·و التقارير — التنظيم والتقارير الثلاثة الجديدة

**`REPS` مرتّبة بالسؤال لا بالكيان**: «فلوسي · بضاعتي · يومي · السجل».
قبل 4.19.0 كانت أربعة تقارير عن المزارع والفرق بينها غير واضح من الاسم،
فكان على المستخدم أن يعرف اسم التقرير قبل أن يعرف إجابته.

**`income` — دخل المكتب.** العمولة مسجَّلة في كل تصفية منذ البداية ولم يكن
هناك تقرير يجمعها: كان بالإمكان رؤية عمولة تصفية واحدة واستحال معرفة دخل
الشهر. وهذا هو الدخل الوحيد في تجارة بالعمولة.
- **مصاريف المزارع ليست تكلفة على المكتب** — تُسترد من المزرعة عند التصفية
  (تُخصم داخل `payable`). المحسوب على المكتب هو **شراء البكش** (`purchase_in`
  بحقل `cost`): أصل يشتريه المكتب بفلوسه.
- عمود **«النسبة» هو المتحقّق فعلاً لا المتفق عليه**: العمولة تُعدَّل يدوياً
  في التصفية (`commissionTouched`)، فقد تعمل بـ٣٪ وأنت تحسبها ٥٪.

**`aging` — أعمار الديون.** الرصيد يقول «كم» ولا يقول «منذ متى».
- **FIFO**: كل تحصيل يُطفئ أقدم فاتورة أولاً، والباقي يُصنَّف بعمره من
  `F.to` (أو اليوم). الشرائح: ٠–٧ · ٨–٣٠ · ٣١–٦٠ · +٦٠.
- الرصيد الافتتاحي (`opening_cash`) يُعامل كأقدم بند.
- **المسوّدات مستبعدة** — ليست ديناً بعد.
- الفائض من التحصيل = **دفعة مقدّمة** ويُعرض منفصلاً (قاعدة ٨).
- يعرض **تركّز المخاطر**: نسبة أكبر ٣ مبايع من الإجمالي.

**`boxLoss` — فاقد البكش.** أصل يُشترى بفلوس ولم يكن له رقم إجمالي.
- الداخل (شراء + افتتاحي + استبدال) مقابل الخارج (تدوير + `#LOSS` + خارج
  السجل) مقابل الموجود (مخزن + عهدة). نسبة الفاقد من الداخل، وتحذير فوق ٥٪.
- **الفاقد هنا هو المسجَّل بحركة «خروج بدون متلقي» لا فرق حسابي**: السجل
  مزدوج القيد بالأرجل فالميزان مقفول دائماً بالبناء، وأي بكش خرج ولم يعد
  يبقى في عهدة الجهة حتى يُجرد.

**المقارنة بالمدة السابقة** عبر `prevSpan(from,to)` + `cmpLine()` — مدة بنفس
الطول تسبقها مباشرةً. تُرجع `null` للمدى المفتوح (لا مدة سابقة تُقارن).

**تصدير CSV** عبر `htmlToCSV(S.print.html)`: كل تقرير يملأ `S.print.html`
للطباعة أصلاً، فالجداول تُقرأ منه — **نقطة واحدة تغطّي كل التقارير** بدل
مُصدِّر لكل تقرير. `\ufeff` في أول الملف ليقرأ Excel العربية صحيحة.

### ٥.٥ الخطوط وسلّم المقاسات

**IBM Plex Sans Arabic** للنص و **IBM Plex Mono** للأرقام (جدولية).
تُحمَّل من Google Fonts مرة واحدة ثم **يخزّنها عامل الخدمة** فتعمل بدون إنترنت.

- **لا `letter-spacing` على العربية إطلاقاً** — يفكّ الاتصال بصرياً.
- ارتفاع السطر ١٫٦٥ · أهداف اللمس ٤٤–٥٢ بكسل.

> **كل مقاس خط في التطبيق من ٦ درجات، بلا استثناء واحد.**
> كان في الملف ٢١ حجم مختلف (11 · 11.5 · 12 · 12.5 · 13 · 13.5 · 14 …).
> العين لا تقرأ الفرق بين 13 و13.5 كفرق معنى، بل كعشوائية.

| المتغيّر | القيمة | الاستعمال |
|---|---|---|
| `--fs-xs` | 12px | وسوم · رؤوس جداول · سطور مساعدة |
| `--fs-s` | 13.5px | نص ثانوي · أزرار صغيرة · عناوين فرعية |
| `--fs-m` | 15px | **حجم الأساس** ونص الجداول |
| `--fs-l` | 17px | عناوين · أزرار أساسية · **حقول الإدخال** |
| `--fs-xl` | 21px | أرقام بارزة |
| `--fs-2xl` | 27px | رقم البطاقة الرئيسي |

- **`--fs-l` لا ينزل عن 16px أبداً**: iOS يكبّر الصفحة تلقائياً عند التركيز على
  حقل أصغر من ١٦، فتقفز الشاشة أثناء الإدخال.
- **صناديق الأيقونات خارج سلّم النص:** `--ic-sm:18` · `--ic-md:20` · `--ic-lg:22`
  · `--ic-xl:34`. حجم الأيقونة ليس حجم نص فلا يخضع لنفس السلّم.
- **المسافات مضاعفات ٤** (`--s1`…`--s6` = 4…24). القيم ١–٣ بكسل مستثناة:
  حشوات داخل الوسوم الصغيرة، والشبكة لا معنى لها عندها.
- **حدّ أو ظل، لا الاثنان.** الأسطح الثابتة (`.card` `.tile` `.hcell` `.attn`
  `.rephead`) بحدّ فقط. الظل للطافي وحده: الشيت · الزر العائم · التوست ·
  شريط الحفظ الملتصق. حدّ + ظل على كل بطاقة = ضوضاء بصرية بلا معلومة.
- **سطر «نسخة X» في الترويسة يظهر في الرئيسية والإعدادات فقط** — للتشخيص لا
  للعمل اليومي، وإخفاؤه يقصّر الترويسة في شاشات التسجيل والتقارير.
  (`.hdr-sub:empty{display:none}` — لا تحذف هذه القاعدة.)

### ٥.٥·أ اختيار الجهة — `entPick`

طريقة **واحدة** لاختيار أي جهة في التطبيق كله. (كان في الملف مُختاران:
`picker()` غير مستعمل إطلاقاً + ورقة بحث في حوار — الاثنان أُزيلا في 4.10.0.
**لا تبنِ ثالثاً.**)

| الحالة | المعروض |
|---|---|
| **≤ ٢٤ جهة** (`PICKMAX`) | كل الأسماء أزراراً — لمسة واحدة تختار |
| **> ٢٤ جهة** (المزارع) | صندوق بحث + **آخر ٣** اتُعومل معهم (`PKRECENT`) |

خياران للحالات الخاصة:
- **`o.items`** — قائمة جاهزة بدل قائمة النوع كلها (مثال: المزارع ذات الدورات
  المفتوحة في نموذج التصفية). تُكتب معرّفاتها في `data-only` **ليبقى البحث
  محبوساً داخلها** فلا يُخرج مزرعة مقفولة.
- **`o.sub`** — سطر فرعي تحت الاسم (مثال: تاريخ بدء الدورة)، والزر يأخذ
  `.pchip.two`.

> كانت هناك دالة ثانية `entPickList` تفعل ذلك بلا بحث ولا «آخر ٣» — أُزيلت في
> 4.11.0 وصارت خيارين هنا. **مُختار واحد فقط في التطبيق.**

- **الأزرار في شبكة `grid` بأعمدة متساوية**، لا `flex-wrap`. الأسماء مختلفة
  الطول فالصف المتعرّج كان يعطي أزراراً بأحجام غير متساوية. الاسم الطويل
  يُقصّ بثلاث نقط، والكامل في `title` وفي البحث.
- **`.pchip` ليس `display:flex`** — `text-overflow:ellipsis` لا يعمل على حاوية
  flex، فيُقصّ الاسم بلا نقط ولا يبدو ناقصاً. `display:block` + `line-height`.
- **`[hidden]{display:none!important}` قاعدة لازمة** بعد قواعد `display`:
  تحديد `display` صراحةً يتغلّب على `[hidden]` الافتراضي، فيظهر صف المختصرات
  ونتائج البحث معاً.
- **البحث لا يستدعي `render()`** — `pkFilter()` يكتب في `.pkres` مباشرةً.
  `render()` يهدم الـDOM، فكان ما كُتب في باقي النموذج يضيع مع كل حرف.
- **حقل البحث بلا `name`** فلا يدخل `readForm` ولا حفظ حالة النموذج.
- «إنتر» يختار أول نتيجة ولا يرسل النموذج.
- `setPick` يفحص وجود الزر **داخل `.pkrow` وحده**: الموجود في نتائج البحث
  لا يُحتسب لأن النتائج تُمسح، فيبدو الحقل فارغاً بعد الاختيار.

### ٥.٥·ب الحركات الملغاة — مخفية افتراضياً

الإلغاء لا يمسح من السجل (قاعدة السجل التراكمي)، لكن المستخدم يلغي الحركة
**لتختفي من شغله**، فبقاؤها في القوائم يجعله يعدّها مرتين.

- `S.showVoid` **في الذاكرة فقط** ويعود «مخفي» مع كل فتحة للتطبيق. لم يُحفظ في
  `meta` عمداً: مفتاح مراجعة نادر لو بقي مفعّلاً نُسي وعاد الازدحام.
- `notVoid(list)` و`voidCount(list)` و`voidBtn(n)` — **نقطة واحدة** تمرّ عليها
  كل القوائم: `repAll` · `logList` · `boxLog` · `repBoxAll`. (`lastOps` كانت
  تُخفيها أصلاً.)
- **زر «الملغاة (ن)»** يظهر في عنوان القسم **فقط إن وُجدت ملغاة فعلاً** — زر
  بلا وظيفة يزحم الشاشة.
- **أعداد الوسوم تتبع المفتاح** أيضاً، وإلا قال الوسم «فواتير (٩)» وعرض ٧.
- الإخفاء **بصريّ بحت**: `derive()` لم يتغيّر ولا حرف، والأرصدة كما هي.

### ٥.٦ مساعدات الإدخال

- **آخر ٣ عمليات فوق كل نموذج**
- **مُختار الجهات:** ≤٢٤ جهة → كل الأسماء أزرار · >٢٤ → آخر المستخدمة + بحث
- **تبويب مجموعات** للجهات المختلطة (مبايع/مزارع) لأن الأسماء قد تتشابه
- **اقتراحات أعداد البكش:** ٧٠ و٨٤ دائماً + الأكثر استخداماً
- **«تسجيل + واحدة تانية»** يُبقي المزرعة والسائق والتاريخ
- **تحذير الفاتورة المكرّرة**
- **🎤 إدخال بالصوت للمبالغ** (ar-EG، يختفي إن لم يدعمه المتصفح)
- **الأرقام العربية ٠١٢٣ تُطبَّع تلقائياً** في الحقول الرقمية
- **خطأ التحقّق يظهر عند الحقل نفسه** عبر `fieldErr(form,name,msg)`: إطار أحمر +
  `aria-invalid` + تمرير الشاشة للحقل + تركيز، وتُمسح العلامة مع أول كتابة.
  مُختار الجهات إدخاله مخفي، فتُعلَّم الحاوية `.pick` بدلاً منه.
- **اهتزاز** ١٢ms مع النجاح و`[24,42,24]` مع الخطأ — مربوط داخل `toast()` نفسها
  فيغطّي كل مسارات الحفظ. مفتاح إيقاف في الإعدادات (`meta.haptics`).
- **وميض آخر حركة** في «آخر ٣»: الصف الجديد يُوسم `l3new` ويكتب «دلوقتي»،
  وعليه زر **تراجع** لمدة ٢٠ ثانية (انظر ٤.١٠).
- **زر الحفظ `position:sticky`** فوق شريط التنقّل — النماذج أطول من الشاشة.
  الزر العائم يرتفع تلقائياً حين توجد استمارة (`body[data-hasform]`).

### ٥.٧ الأمان والصيانة

- **رقم سري للأونر** يحمي: التصفية · التراجع · التعديل · الحذف · الجرد · الاستعادة · التصفية النهائية
- **قفل الفترات** حتى تاريخ
- **فحص سلسلة التوقيع** · **٦٢ اختباراً ذاتياً** لدالة `derive`
- **نسخ احتياطي** تلقائي كل ٥٠ حركة + بانر تحذير + استعادة بالدمج
- **تصفية الحسابات** على ٤ خطوات (نطاق → نسخة → كلمة «تصفية» → رقم سري) مع سجل أثر
- **زر تحديث قسري** يمسح الكاش دون لمس البيانات
- **تشخيص التثبيت** يفحص شروط كروم ويعرض الروابط الناقصة

---

## ٦. القرارات التقنية وأسبابها

| القرار | السبب |
|---|---|
| **ملف واحد بلا فريمووركس** | قيد أصلي من المالك. يسهّل الرفع من الموبايل ويلغي خطوة البناء. |
| **Append-only بدل CRUD** | الأرقام مصدر خلاف في هذه التجارة. أي رقم يجب أن يُتتبَّع لحركة مؤرّخة. |
| **التعديل والحذف كحركات جديدة** | المالك طلب تعديلاً وحذفاً، والتنازل عن الـ append-only كان سيهدم الأساس. الحل: الواجهة تبدو CRUD والداخل تراكمي. |
| **`derive()` دالة نقية** | تجعل الاختبار رخيصاً — ٦٢ اختباراً تعمل بلا متصفح ولا قاعدة بيانات. |
| **المال بالقروش الصحيحة** | تجنّب انحراف العشري في المجاميع. |
| **نموذج الأرجل للبكش** | يعبّر عن كل عملية (صرف · استبدال · جرد · فاقد) بقاعدة واحدة، ويجعل الرصيد مجموعاً بسيطاً. |
| **المواقع الافتراضية `#`** | تُقفل المعادلة: لا صندوق يظهر أو يختفي بلا طرف مقابل، حتى لو اسم الطرف «فاقد». |
| **عقدة `#ADJ` لتحويل الحالة** | الرجل الواحدة تحمل حالة واحدة، فتحويل سليم→تالف يحتاج عقدة وسيطة. |
| **العهدة سليم فقط** | الصندوق خرج سليماً فلا يصحّ أن يُقيَّد تالفاً على من استلمه سليماً. |
| **عدّاد التالف ليس مديونية** | طلب صريح من المالك: متابعة أمانة لا مطالبة مالية. |
| **الفاتورة المسوّدة تنقل البكش** | البكش تحرّك فعلاً، والمبلغ وحده هو المعلّق. |
| **ملفات مسطّحة بلا مجلدات** | GitHub لا يحافظ على المجلدات في الرفع من الموبايل. |
| **أيقونات data-URI في المانيفست** | نسخة احتياطية إن لم تُرفع ملفات الأيقونات. |
| **رقم النسخة ظاهر في الترويسة** | الكاش سبّب دورة تشخيص طويلة؛ الرقم الظاهر يقطع الشك فوراً. |
| **مستمع نقر واحد على `document`** | زر خارج الحاوية لم يكن يستجيب. مستمع واحد يقفل هذا النوع من الأخطاء. |
| **حارس `_lastEv`** | يمنع تنفيذ الأمر مرتين إن تعدّدت المستمعات. |
| **IBM Plex Sans Arabic** | خطوط الهواتف الافتراضية للعربية رفيعة ومنخفضة التباين وتُقرأ بصعوبة في ضوء السوق. |
| **إلغاء صبغة الخلفية للأقسام** | تلوين الشاشة كاملة يجعل التطبيق غير مستقر ويفقد اللون معناه. |
| **أيقونات SVG بدل الإيموجي** | الإيموجي يختلف بين الأجهزة ويفرض ألواناً تكسر قاعدة «اللون نادر ومعناه ثابت». |
| **الاهتزاز داخل `toast()`** | مكان واحد يغطّي كل مسارات الحفظ بدل تعديل كل نموذج — والتوست وحده لا يصل في سوق مزدحم. |
| **خطأ التحقّق عند الحقل** | التوست يظهر أسفل الشاشة والحقل الخاطئ قد يكون خارجها، فيدور المستخدم. |
| **التراجع بلا رقم سري** | الشرط (آخر حركة، نفس الجهاز، ٢٠ ثانية) أضيق بكثير من الحذف العام، والفعل نفسه شاهدة إلغاء مسجَّلة. |
| **رأس الجدول `sticky` خارج `.tscroll`** | الحاوية ذات `overflow-x` تصير scroll container فيُحسب الـsticky بالنسبة لها ولا يعمل. |
| **قياس `--hdrh` عند الرسم** | ارتفاع الترويسة متغيّر (اسم طويل/سطر النسخة)، فالرقم الثابت يترك فجوة أو يقصّ. |
| **سلّم مقاسات بمتغيّرات لا بأرقام** | الرقم الخام يعود يتكاثر مع كل تعديل. المتغيّر يجعل الخروج عن السلّم ظاهراً في المراجعة، ويختبره `v12` آلياً. |
| **`.btn{line-height:1.3}`** | ارتفاع السطر ١٫٦٥ مع مقاس ١٧ يطوّل كل زر ٦ بكسل بلا داعٍ، والشاشة صغيرة. |
| **تنظيم التقارير بالسؤال** | التنظيم بالكيان يفرض على المستخدم معرفة اسم التقرير قبل أن يعرف إجابته. |
| **CSV من `S.print.html`** | كل تقرير يملأه أصلاً، فمُصدِّر واحد يغطّي الجميع والجديد منها تلقائياً. |
| **الفاقد = المسجَّل لا الفرق الحسابي** | السجل مزدوج القيد فالميزان مقفول بالبناء؛ عرض «فرق» سيكون صفراً دائماً ويوهم بالسلامة. |
| **الصفر يُعرض كعلامة لا كرقم** | أكبر خط في السطر كان يحمل أقل معلومة، وقائمة صفوف متطابقة تُمسح بلا فائدة. |
| **«واحدة تانية» هو الأساسي في الفاتورة** | المسار الطبيعي لا الاستثناء؛ كونه `ghost` كان يقول للمستخدم إنه الخيار الثاني. |
| **حقل التاريخ مخفي لا مبنيّ عند الطلب** | بناؤه عند الضغط يحتاج `render()` وهو يهدم النموذج ويضيّع ما كُتب فيه. |
| **العدّاد الأحمر خلف الطيّة** | الطيّ يخفي البنود، فبلا إشارة لونية يمرّ «رصيد المخزن بالسالب» أياماً دون أن يُرى. |
| **الطيّ عبر `S.drill` لا آلية جديدة** | آليتا طيّ في شاشة واحدة = سلوكان يفترقان مع أول تعديل. |
| **بحث داخل المُختار لا في حوار** | الحوار طريقة ثانية لنفس الشغلانة، وطريقتان = منطق مكرّر واختلاف سلوك بين الشاشات. |
| **إخفاء الملغاة افتراضياً لا حذفها** | السجل تراكمي فالحذف ممنوع، لكن عرضها في قوائم الشغل يجعل المستخدم يعدّها مرتين. |
| **`showVoid` في الذاكرة لا في `meta`** | مفتاح مراجعة نادر: لو حُفظ مفعّلاً نُسي وعاد الازدحام من حيث لا يدري. |
| **تقرير السائقين أقساماً لا جدولاً واحداً** | جدول مختلط لكل السائقين رصيده الجاري بلا معنى — يجمع سائقين مختلفين في عمود واحد. |
| **إلغاء «الأرصدة» التراكمية من تقرير السائقين** | بطلب المستخدم: التقرير أسبوعي والرقم التراكمي كان يُقرأ خطأً كمستحق الأسبوع. الأرصدة الكلّية باقية في شاشة العمليات ← السائقين. |

---

## ٧. الحاجات اللي لسه مش خالصة

| البند | الحالة | ملاحظات |
|---|---|---|
| **المزامنة بين الأجهزة** | ⚠️ جزئي | الأساس جاهز (`deviceId`, `opId`, دمج بلا تعارض بالملف). المزامنة الحقيقية تحتاج سيرفر. السجل تراكمي فالدمج CRDT-ish مجاناً. |
| **PDF حقيقي** | ❌ | الطباعة عبر `window.print()` فقط. PDF مباشر يحتاج pdfmake + خط عربي مضمّن (~٦٠٠ ك.ب). |
| **إرسال ملف على واتساب** | ❌ مستحيل من المتصفح | يُرسل نص منسّق فقط. `navigator.share` مع ملف ممكن لو توفّر PDF. |
| **الخطوط أوفلاين من أول لحظة** | ⚠️ | تُحمَّل من Google Fonts أول مرة ثم تُخزَّن. خطوات الاستضافة الذاتية في README. |
| **الأداء مع >٢٠ ألف حركة** | ⚠️ | `derive()` يعيد الحساب كاملاً. الحل عند الحاجة: snapshot كل ١٠٠٠ حركة + إعادة تشغيل الذيل. |
| **صلاحيات متعددة المستخدمين** | ⚠️ | رقم سري واحد للأونر. لا حسابات منفصلة. |
| **قفل اليوم التلقائي** | ❌ مرفوض من المالك | قفل الفترات اليدوي موجود. |
| **تنبيه العهدة الكبيرة** | ❌ مرفوض من المالك | «العهدة الراكدة» (١٤ يوم) مُنفَّذ بدلاً منه. |
| **رأس جدول ملتصق داخل `.tscroll`** | ❌ | مستحيل بـCSS وحده: الحاوية scroll container. يحتاج جدولاً مزدوجاً أو JS. |
| **تقسيم الكود لملفات** | ❌ | ٥٠٨٠ سطر في ملف واحد. الاقتراح: مصدر مقسّم + esbuild يدمج، مع بقاء التسليم ملفاً واحداً. |
| **`html` tagged template للتهريب** | ⚠️ | الحماية حالياً بـ `esc()` + تنظيف عند حدود الكتابة (`clean()` يمنع `<` و `>` من دخول المخزن). |

---

## ٨. الأخطاء اللي وقعنا فيها وحلها

> **هذا القسم هو أهم ما في الوثيقة.** معظم هذه الأخطاء تكرّرت لأن التنفيذ تمّ بلا فحص للحالة الفعلية للملف.

### ٨.١ أخطاء منطقية

| الخطأ | الأثر | الحل |
|---|---|---|
| `POSF` كانت تشترط `amount > 0` للفاتورة | **المسوّدة مرفوضة** رغم بناء الميزة كاملة | حذف `invoice` من `POSF` |
| عدّ صناديق `return_in` بجمع كل الأرجل | ٧٠+١٠ يظهر **٩٠** بدل ٨٠ (عقدة `#ADJ` تعدّ الصندوق مرتين) | `boxQty()` تحسب من `params` أو تستبعد أرجل `#ADJ` |
| التالف الراجع يُخصم من رصيد **التالف** عند الجهة | رصيد تالف سالب عند مبيع لم يستلم تالفاً أصلاً | قاعدة «العهدة سليم فقط» + تمرير الحالة عبر `#ADJ` |
| مصنع الثلج كان هدف `factory_replace` | خلط مجال الثلج بمجال البكش | فصل: مصدر استبدال غير مسمّى (`#SUPPLY`) |
| العمولة كانت `a/(a+b)` | ٥/٧٠ تعطي ٦٫٦٧٪ بدل ٧٫١٤٪ | كسر حقيقي `a/b` |
| `custody.factories` بعد إلغاء موردي البكش | اختبار يفشل بـ NaN | تحديث إلى `custody.sups` ثم حذفه |

### ٨.٢ أخطاء واجهة

| الخطأ | الأثر | الحل |
|---|---|---|
| **مستمع النقر يُعلَّق داخل `render()`** | يتراكم مع كل رسم فيُنفَّذ الأمر مرات | تعليق **مرة واحدة** على `document` |
| **الزر العائم خارج `#app`** | لا يستجيب إطلاقاً — المستمع كان على `#app` فقط | مستمع على `document` |
| **`onclick` مباشر + مستمع مستند** | الضغطة تنادي الدالة **مرتين**: تفتح وتقفل فوراً | إزالة `onclick` + حارس `_lastEv` |
| **حالة القائمة في صنف CSS** | `render()` كان يمسحها فتُقفل عند أي تحديث | الحالة في `S.fab` ومصدر واحد للحقيقة |
| `render()` يبني `innerHTML` كاملاً | فقدان المكتوب والتركيز وموضع التمرير | التقاط حالة النموذج المُركَّز وإرجاعها بعد الرسم |
| `maximum-scale=1` في viewport | منع التكبير — مشكلة وصولية | إزالته |
| `confirm`/`prompt` الأصلية | تُحجب في PWA المثبَّت · اتجاه لاتيني · الرقم السري مكشوف | حوارات داخل التطبيق بحقل `password` |
| صبغة خلفية كاملة لكل قسم | الشاشة تبدو غير مستقرة واللون يفقد معناه | هوية القسم في الشريط والأيقونة فقط |

### ٨.٣ أخطاء نشر (استهلكت أطول وقت)

| الخطأ | التشخيص | الحل |
|---|---|---|
| **فتح الملف من `file://`** | المتصفحات تمنع IndexedDB — الكتابة تفشل **بصمت** فتبدو الإضافة معطّلة | فحص تخزين عند الإقلاع يشرح السبب صراحةً |
| **`sw.js` غير مرفوع** | كروم يرفض التثبيت ويعمل اختصار | تشخيص تثبيت يعرض **الرابط الناقص بالضبط** |
| **الأيقونات في الجذر والمانيفست يطلبها في `icons/`** | فشل شرط الأيقونات | تسطيح كل الملفات + أيقونات data-URI احتياطية |
| **`id` نسبي في المانيفست** | يُحلّ من أصل الموقع فيخرج خارج النطاق ويربك كروم | حذف `id` — كروم يشتقّه من `start_url` |
| **نسخة مخزَّنة في الكاش** | المستخدم يبلّغ عن أخطاء مُصلَحة | **رقم النسخة ظاهر في الترويسة** + زر تحديث قسري |

### ٨.٤ أخطاء تنفيذ متكرّرة (درس للمساعد القادم)

| النمط | المثال | الوقاية |
|---|---|---|
| **إضافة ميزة موجودة أصلاً** | المُختار بمجموعات · الميكروفون · المقارنة بالفترة السابقة · قسم الانتباه — كلها نُفِّذت مرتين وتسبّبت في `Identifier already declared` ودوال غير معرّفة | **`grep` عن الميزة قبل بنائها** |
| **افتراض نص الكود من الذاكرة** | `str_replace` يفشل بسبب مسافة بادئة أو `async` مفقود | **قراءة الموضع الفعلي أولاً** |
| **`assert` يوقف السكربت قبل الحفظ** | تُفقد كل التعديلات الناجحة في نفس الدفعة | تجميع النتائج والإبلاغ عن الفاشل مع الحفظ دائماً |
| **توقّعات اختبار قديمة** | تغيير مفتاح `S.sub.rep` → `S.rep` كسر ٣ سويتات | تحديث الاختبارات مع كل تغيير هيكلي |

---

## ٩. الاختبارات

تعمل بـ Node بلا متصفح على شيم في `shim.js` (DOM + IndexedDB + crypto).

> **ملاحظة مهمة:** ملفات السويتات **ليست داخل المستودع** — كانت تُبنى في كل جلسة.
> في 4.8.0 أُعيد بناء الشيم و٤ سويتات (`runtests` · `run` · `v10` · `v11`).
> `shim.js` يجعل `querySelector` يرجع `null` لغير المعرّفات (كالمتصفح)، وإلا
> نُفِّذت دوال ربط لعناصر غير موجودة وسقطت الاختبارات بخطأ وهمي.

السويتات:

| السويت | تغطّي |
|---|---|
| `runtests` | **٦٢ اختباراً** داخل التطبيق لدالة `derive` (تظهر أيضاً في الإعدادات) |
| `run` · `setg` · `setlist` | رسم كل الشاشات وأنواع البيانات |
| `box` · `boxrep` · `cust` | تبويبات البكش · كشف العهدة · قاعدة السليم/التالف |
| `dmg` · `wipe` · `grp` · `pick` | التالف · التصفية النهائية · المجموعات · المُختار |
| `rep` · `v3`–`v9` · `fab` | التقارير · الميزات المتراكمة · الزر العائم |
| `v10` | الأيقونات · الاهتزاز · `fieldErr` · وميض آخر حركة والتراجع · محاذاة الجداول (٤٣) |
| `v11` | شريط الحفظ الملتصق · رأس الجدول · الوضع الغامق · باقي الأيقونات (٢٥) |
| `v12` | سلّم المقاسات · شبكة المسافات · الظل · ترويسة أقصر (٢٤) |
| `v13` | بحث المُختار · تساوي الأزرار · تقرير السائقين لكل سائق (٥١) |
| `v14` | إخفاء الملغاة ومفتاحها · المُختار بقائمة مخصّصة (٣٦) |
| `v15` | تقرير مصاريف المزارع اليومي (٣٠) |
| `v16` | ورقة التسليم: تفصيل المزارع + التحصيلات · `farmSpan` (٥٦) |
| `v17` | صافي حساب المبايع آخر اليوم · `merchAsOf` (٣٤) |
| `v18` | طيّ «محتاج انتباهك» وتجميع المزارع المصفّاة (٣٢) |
| `v19` | صف التسجيل · أفعال الزر العائم · التاريخ والملاحظة المطويّان (٤٦) |
| `v20` | دخل المكتب · أعمار الديون · فاقد البكش · CSV (٦٢) |
| `v21` | قائمة العهدة: النشط فقط والفاقد متجمّع (٢١) |
| `ver` | `APPVER` = `sw VERSION` = نسخة الوثيقة (٤) |

**قبل أي تسليم:** شغّل الكل وتأكد من صفر أخطاء و`62/62`.

---

## ١٠. النشر

1. مستودع GitHub **Public**.
2. ارفع **٦ ملفات في مستوى واحد**: `index.html`, `sw.js`, `manifest.json`, والأيقونات الثلاث.
3. **Settings ← Pages ← main / root**.
4. افتح الرابط من الموبايل ← **«تثبيت التطبيق»** (لا «إضافة إلى الشاشة الرئيسية»).

> **مع كل رفع: غيّر `APPVER` في `index.html` و `VERSION` في `sw.js`.**
> بدونهما يبقى المستخدمون على النسخة المخزَّنة، وهو أكثر مصدر لبلاغات وهمية.

---

## ١١. سجل الدفعات

| النسخة | التاريخ | المحتوى |
|---|---|---|
| **4.20.0** | ٢٠٢٦-٠٨-٢٩ | قائمة العهدة في الرئيسية: النشط فقط، والجهات ذات الفاقد بلا عهدة في سطر واحد يُفتح بالضغط |
| **4.19.0** | ٢٠٢٦-٠٨-٢٩ | ثلاثة تقارير جديدة: دخل المكتب · أعمار الديون · فاقد البكش · إعادة تنظيم `REPS` بالسؤال · مقارنة بالمدة السابقة · تصدير CSV لكل التقارير |
| **4.18.0** | ٢٠٢٦-٠٨-٢٨ | مسار تسجيل أقصر: صف تسجيل في الرئيسية · أفعال بدل أقسام في الزر العائم · التاريخ والملاحظة مطويّان · «واحدة تانية» أساسي · مبالغ تحصيل سريعة · الصفر علامة لا رقم · `aria-live` للتوست |
| **4.17.0** | ٢٠٢٦-٠٨-٢٨ | الرئيسية: طيّ «محتاج انتباهك» (بعدّاد أحمر للبنود الخطيرة) · تجميع المزارع المصفّاة في سطر واحد يُفتح بالضغط · حذف قاعدة `.attn` مكرّرة ميتة |
| **4.16.0** | ٢٠٢٦-٠٨-١٦ | بكش المزرعة في جدول مميّز بلون البكش (صادر · وارد · الباقي عليها) · جدول المبايع عمودان: الاسم والصافي مع وسم المسوّدات |
| **4.15.0** | ٢٠٢٦-٠٨-١٥ | ورقة التسليم تبدأ من المزارع مباشرةً (إزالة المؤشّرات والمقارنة والأرصدة العامة، ونقل «محتاج مراجعة» لآخرها) · بكش المزرعة رقمان: صادر للمزرعة ووارد منها |
| **4.14.0** | ٢٠٢٦-٠٨-١٥ | إضافة «صافي حساب المبايع آخر اليوم» لذيل ورقة التسليم — شاشةً وطباعةً |
| **4.13.0** | ٢٠٢٦-٠٨-١٥ | ذيل ورقة تسليم اليوم: تفصيل كل مزرعة (بيع · ثلج · مصاريف · بكش) ثم التحصيلات — على الشاشة وفي الطباعة · توحيد الحساب في `farmSpan`/`colSpan` وإزالة تكرار في `repFarmDaily` |
| **4.12.0** | ٢٠٢٦-٠٨-١٥ | تقرير جديد: مصاريف المزارع اليومي (مجمّع بالبنود + تفصيل لكل مزرعة) |
| **4.11.0** | ٢٠٢٦-٠٨-١٥ | إخفاء الحركات الملغاة في كل القوائم مع مفتاح إظهار · توحيد `entPickList` داخل `entPick` (قائمة مخصّصة + سطر فرعي) |
| **4.10.0** | ٢٠٢٦-٠٨-١٠ | بحث داخل مُختار المزارع + آخر ٣ · أزرار الاختيار في شبكة متساوية · تقرير السائقين أقساماً لكل سائق وإلغاء «الأرصدة» · حذف مُختارين ميتين |
| **4.9.0** | ٢٠٢٦-٠٨-١٠ | دفعة واجهة ٣: سلّم ٦ مقاسات (بدل ٢١) · مسافات على شبكة ٤ · إزالة الظل عن الأسطح الثابتة · سطر النسخة في الرئيسية والإعدادات فقط |
| **4.8.0** | ٢٠٢٦-٠٨-١٠ | دفعة واجهة ٢: شريط حفظ ملتصق · رأس جدول ملتصق · وضع غامق يدوي · تحويل باقي الإيموجي لأيقونات (٢٦ أيقونة) |
| **4.7.0** | ٢٠٢٦-٠٨-١٠ | دفعة واجهة ١: أيقونات SVG للتنقّل والزر العائم · اهتزاز · خطأ عند الحقل · وميض آخر حركة + تراجع ٢٠ ثانية · محاذاة الجداول |
| 4.6.3 | — | آخر نسخة قبل دفعات الواجهة (الوثيقة كانت متوقّفة عند 4.1.0) |

> عند كل دفعة: **حدّث هذا الجدول** ورقم النسخة أعلى الوثيقة، وأضف أي قاعدة
> جديدة في القسم ٤ وأي خطأ وقعت فيه في القسم ٨. الوثيقة تصف الحالة الفعلية،
> لا النوايا — فلا تكتب فيها ما لم يُنفَّذ.
